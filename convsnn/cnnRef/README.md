1. 测器件
   得到 LTP 曲线：脉冲数 → 电导增强
   得到 LTD 曲线：脉冲数 → 电导减弱

2. 建 LUT
   把 “当前电导 + 目标变化量” 映射成 “需要几个脉冲”

3. 训练网络
   forward 用当前实际电导 G 计算输出

4. backprop 得到梯度
   grad = ∂L/∂w

5. 理想权重更新
   Δw = -η \* grad

6. 把 Δw 转成 ΔG

7. 查表决定打几个 LTP/LTD 脉冲

8. 用实测曲线更新实际 G

9. 再把实际 G 映射回实际权重 w_hw
