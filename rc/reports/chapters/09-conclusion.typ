#import "../template.typ": *
#import "../icons.typ": *

= 总结与展望

== 工作总结

本项目完成了从 PlantVillage 静态叶片图像到光电储备池时间序列的完整计算链。系统通过固定空间分块和蛇形扫描保留二维邻域关系，通过 RGB tonic、双极 ON/OFF 事件与局部纹理联合描述颜色和结构，再利用 12×12 异构动态阵列将 64 帧输入投影为可供线性分类器读取的高维状态。

从 v2 到 v5 的实验表明，提升空间分辨率、加入互补纹理和增加状态可观察性均能提高完整 38 类验证性能。其中 v5 达到 79.91% 验证准确率和 77.23% 宏召回率，但未达到 80% 宏召回率门槛，因而没有评估冻结测试集。筛选十分类 v5 通过门槛，并取得 97.02% 测试准确率和 96.80% 测试宏召回率。

项目还形成了可直接交接给硬件端的数据包，包括九路连续输入、九路二值兼容输入、十二路连续输入、类别映射、样本索引、SHA-256 校验以及逐帧播放规则。这使软件仿真与器件实验之间具有明确的数据契约。

== 主要结论

#grid(
  columns: (1fr, 1fr),
  gutter: 8pt,
  block(fill: colors.green-soft, radius: 4pt, inset: 9pt)[#icon-label(grid-icon(), [空间信息不可忽略], [8×8 扫描显著优于粗粒度终态表示])],
  block(fill: colors.blue-soft, radius: 4pt, inset: 9pt)[#icon-label(texture(), [纹理与颜色互补], [三路纹理改善病斑结构可分性])],
  block(fill: colors.orange-soft, radius: 4pt, inset: 9pt)[#icon-label(readout(), [状态可见性是关键], [线性读出受益于检查点和完整轨迹])],
  block(fill: colors.green-soft, radius: 4pt, inset: 9pt)[#icon-label(chip(), [接口已经可交付], [算法参数与物理标定边界清晰])],
)

== 局限性

第一，PlantVillage 图像多为受控背景，性能不能直接外推到复杂田间环境。第二，当前动态阵列仍是一级软件仿真，未覆盖真实器件中的噪声、漂移、饱和和通道不一致。第三，筛选十类由早期验证结果确定，较高指标包含类别选择条件。第四，v5 的电读次数和 9216 维状态会增加采集、存储与读出训练成本。第五，当前未形成最终 v5 混淆矩阵图片，也未完成真实硬件功耗、延迟与重复性测量。

== 后续工作

后续工作建议按以下优先级推进：

1. 完成真实器件输入范围、时间尺度和复位条件标定；
2. 使用预览数据建立端到端采集链并验证 64 帧顺序；
3. 重复采集同一样本以量化噪声、漂移和日间差异；
4. 基于真实状态重新训练十分类线性读出并评估冻结测试集；
5. 对读取次数进行 8/16/32/64 消融，寻找性能与能耗折中；
6. 生成 v5 预测向量、最终混淆矩阵和错误样本分析；
7. 在不使用测试集选择模型的前提下扩展到完整 38 类；
8. 引入田间背景数据，评估跨域泛化与背景依赖。

#callout([最终结论], [
  当前成果已经证明了输入编码、动态计算和硬件数据接口的系统可行性；下一阶段的决定性证据应来自真实 12×12 阵列状态，而不是继续扩大软件读出层复杂度。
], tone: "blue")
