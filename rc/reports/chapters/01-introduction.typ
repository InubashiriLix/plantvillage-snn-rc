#import "../template.typ": *
#import "../icons.typ": *

= 绪论

== 研究背景

植物病害会直接影响作物产量和品质，可靠而快速的叶片识别是精准施药、田间监测和农业数字化的重要环节。PlantVillage 开放了超过五万幅健康与病害叶片图像 @hughes2015，已成为植物病害视觉识别的常用基准。Mohanty 等使用该数据集验证了深度卷积网络进行病害分类的可行性，同时也指出受控背景与真实田间环境之间存在明显域差异 @mohanty2016。

深度神经网络能够获得较高识别性能，但其训练和推理通常依赖大量乘加运算、参数存储和数据搬运。对于功耗受限的边缘设备，采用物理动态系统承担时序映射、只训练轻量读出层的储备池计算（Reservoir Computing, RC）提供了不同的实现路径。RC 的核心思想是将输入投影到具有非线性和短期记忆的高维动态状态中，而把训练集中在输出层 @lukosevicius2009。该计算框架尤其适合利用光电、忆阻或其他物理器件的固有动态。

#callout([本项目关注的问题], [
  如何将静态叶片图像转换为适合 12×12 光电动态阵列处理的时序信号，并在有限光学帧数和简单读出层条件下保留足够的颜色、变化与纹理信息。
])

== 研究目标

本项目不以复现一个高参数量视觉网络为目标，而是围绕“输入编码—器件动态—状态读取—硬件交接”建立完整、可验证的系统。具体目标包括：

- 构建固定且可复现的图像分块和蛇形扫描流程；
- 设计 RGB tonic、ON/OFF 事件以及局部纹理联合编码；
- 建立具有异构时间常数的 12×12 储备池仿真模型；
- 比较终态、检查点和逐帧稠密读取的性能与代价；
- 使用宏召回率约束类别不平衡影响，并冻结测试集；
- 导出硬件可直接读取的 CSV/NPY 数据及播放协议。

== 技术贡献

#grid(
  columns: (1fr, 1fr),
  gutter: 8pt,
  block(fill: colors.green-soft, radius: 4pt, inset: 9pt)[
    #icon-label(scan(), [空间—时间转换], [8×8 图像仅需 64 个连续光学帧])
  ],
  block(fill: colors.blue-soft, radius: 4pt, inset: 9pt)[
    #icon-label(event(), [多通路联合编码], [绝对颜色、双极事件与局部纹理])
  ],
  block(fill: colors.blue-soft, radius: 4pt, inset: 9pt)[
    #icon-label(reservoir(), [异构动态映射], [12 行时间常数谱增强时间响应多样性])
  ],
  block(fill: colors.orange-soft, radius: 4pt, inset: 9pt)[
    #icon-label(chip(), [硬件可交付接口], [九路、十二路与二值兼容格式])
  ],
)

本报告所述结果均来自仓库中保留的实验 JSON、分类报告和导出清单。完整 38 类任务与验证集筛选十类任务分别陈述；未通过阶段门槛的实验不报告测试性能；软件仿真指标不表述为真实器件实测指标。

== 报告结构

第 2 章给出系统总体架构和数据流；第 3 章说明图像分块、蛇形扫描与输入编码；第 4 章描述储备池动态和读出层；第 5 章分析 v2 至 v5 的方法演进；第 6 章说明实验设计与数据治理；第 7 章给出完整任务和十分类结果；第 8 章定义硬件接口；第 9 章总结当前结论、局限和后续工作。
