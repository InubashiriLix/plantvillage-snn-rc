#import "../template.typ": *
#import "../icons.typ": *

= 实验设计与评价方法

== 数据划分与测试集冻结

预处理程序将原始训练目录按类别分层拆分为训练集与验证集，并把既有验证目录固定为测试集。阈值、归一化尺度、模型超参数和数据增强策略均由训练/验证数据决定。完整 38 类 v3、v4 和 v5 没有达到各自继续条件或最终 80% 宏召回率门槛，因此没有评估冻结测试集。

这一机制比“每个版本都报告测试结果”更严格，因为它避免测试集在多轮调参中逐渐成为隐式验证集。早期 v2 已产生测试结果，但后续主路线仍按照门槛策略执行，报告中不使用 v2 测试结果为后续方案选择提供反馈。

== 评价指标

设第 $k$ 类真正例、假正例和假负例分别为 $"TP"_k$、$"FP"_k$ 和 $"FN"_k$。类别精确率、召回率及 F1 定义为

$ P_k = "TP"_k/("TP"_k+"FP"_k), quad R_k = "TP"_k/("TP"_k+"FN"_k), $
$ "F1"_k = 2 P_k R_k/(P_k+R_k). $

总体准确率反映全部样本中的正确比例；宏召回率对每一类召回率等权平均：

$ R_("macro")=1/K sum_(k=1)^K R_k. $ <eq-macro>

由于 PlantVillage 各类别样本数不完全一致，项目以验证宏召回率作为主要选择指标，同时报告准确率和预测类别数。预测类别数用于检查模型是否退化到只输出少数多数类。

== 超参数搜索

各版本围绕影响器件动态和读出复杂度的变量进行有限搜索。主要变量包括事件阈值分位数 $q$、最大时间常数 $tau_(max)$、输入增益 $gamma$、Ridge 正则化 $lambda$、Softmax 权重衰减以及状态读取次数。

#academic-table(
  4,
  ([参数], [物理/算法含义], [典型候选], [选择依据]),
  (
    [$q$], [事件死区或训练分位阈值], [0.60、0.70、0.80；连续 q000], [验证宏召回率],
    [$tau_(max)$], [最长记忆时间尺度], [64、128、256], [动态信息保持],
    [$gamma$], [输入到非线性节点的增益], [0.5–3.0], [避免响应过弱或饱和],
    [$lambda$], [Ridge 正则化], [$10^(-4)$–1], [泛化与数值稳定],
    [weight decay], [Softmax 权重衰减], [$10^(-5)$–$10^(-3)$], [验证性能与早停],
    [read count], [状态采样密度], [1、8、16、64], [信息量与硬件成本],
  ),
  widths: (28mm, 56mm, 39mm, 34mm),
)

== 数据增强与候选选择

v5 首先以部分读取状态筛选事件编码和动态参数，再对排名靠前的候选训练稠密线性读出。训练集水平/垂直翻转作为独立候选，只有当验证宏召回率提升时才替代无增强模型。完整 38 类和筛选十分类中，增强模型的验证宏召回率均略低，因此最终模型均使用未增强训练集。

筛选十分类的三个稠密候选中，最终候选并非简单按同一验证集最高准确率选择，而是结合内部训练划分进行候选选择，然后对外部验证集作最终报告。这能减弱直接在最终验证集上重复选择的风险，但类别子集本身仍来自早期验证表现，选择偏差不能消除。

== 可复现性

每个阶段保存预处理配置、固定随机掩码、行动态参数、验证试验表、分类报告和训练模型。硬件导出清单进一步记录每个文件的 shape、字节数和 SHA-256。推荐的最小复现链为：

```bash
python rc/scripts/01_preprocess_dataset.py --patch-grid 8 8 --active-columns 12 \
  --output-dir artifacts/preprocessed_v4
python rc/scripts/06_train_spatial_v4.py --device cuda
python rc/scripts/07_train_dense_v5.py --device cuda
python rc/scripts/08_export_hardware_inputs.py
```

#callout([结果来源优先级], [
  数值指标与最终参数以对应实验目录中的 `*_results.json` 为第一事实来源；CSV 试验表用于追踪候选；README 用于解释流程，但不替代实验产物。
], tone: "blue")
